#include <stdio.h>
#include <stdlib.h>

/**
  * add - function that adds two integers.
  * @a: the first inputed integer.
  * @b: the second inputed integer.
  * Return: the addition of a and b.
  */

int add(int a, int b)
{
	return (a + b);
}
