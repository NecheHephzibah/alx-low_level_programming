#include <stdio.h>
#include <stdlib.h>

/**
  * mod - function that returns the remainder when 
  * wo integers.
  * @a: the first inputed integer.
  * @b: the second inputed integer.
  * Return: the modulus of a and b.
  */

int mod(int a, int b)
{
    return (a % b);
}
